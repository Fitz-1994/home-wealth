spring:
  datasource:
    url: jdbc:mysql://localhost:3306/home_wealth?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: password

logging:
  level:
    com.homewealth: DEBUG
    org.mybatis: DEBUG
