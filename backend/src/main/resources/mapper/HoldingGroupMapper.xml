<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homewealth.mapper.HoldingGroupMapper">

    <resultMap id="GroupMap" type="com.homewealth.model.HoldingGroup">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="groupName" column="group_name"/>
        <result property="note" column="note"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="GroupMap">
        SELECT * FROM holding_group WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="GroupMap">
        SELECT * FROM holding_group WHERE user_id = #{userId} ORDER BY created_at ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO holding_group (user_id, group_name, note)
        VALUES (#{userId}, #{groupName}, #{note})
    </insert>

    <update id="update">
        UPDATE holding_group SET
            group_name = #{groupName},
            note       = #{note},
            updated_at = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM holding_group WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <!-- 成员操作（symbol 维度） -->
    <select id="findMembersByGroupId" resultType="map">
        SELECT group_id AS groupId, symbol FROM holding_group_member WHERE group_id = #{groupId}
    </select>

    <select id="findAllMembersByUserId" resultType="map">
        SELECT m.group_id AS groupId, m.symbol
        FROM holding_group_member m
        INNER JOIN holding_group g ON m.group_id = g.id
        WHERE g.user_id = #{userId}
    </select>

    <insert id="insertMember">
        INSERT INTO holding_group_member (group_id, symbol) VALUES (#{groupId}, #{symbol})
    </insert>

    <delete id="deleteMembersByGroupId">
        DELETE FROM holding_group_member WHERE group_id = #{groupId}
    </delete>

    <select id="countMemberBySymbol" resultType="int">
        SELECT COUNT(*) FROM holding_group_member m
        INNER JOIN holding_group g ON m.group_id = g.id
        WHERE m.symbol = #{symbol} AND g.user_id = #{userId}
    </select>

</mapper>
